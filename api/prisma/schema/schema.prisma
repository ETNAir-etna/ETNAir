generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["fullTextSearchPostgres", "views", "relationJoins", "prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
